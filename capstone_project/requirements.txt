# 데이터베이스 커넥터 (PostgreSQL 및 pgvector 연동)
psycopg2-binary>=2.9.9

# AI 모델 API 통신 (vLLM / Ollama 호환용)
requests>=2.31.0

# PDF 텍스트 및 이미지 영역 추출
pdfplumber>=0.11.0

# MS Office (Word, PowerPoint, Excel) 구조적 파싱
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2

# 이미지 처리 및 VLM 전송용 포맷 변환
pillow>=10.3.0

kiwipiepy

docling
markitdown